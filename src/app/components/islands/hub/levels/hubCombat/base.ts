import { i18n } from '../../../../../lib/i18n/i18n';
import { LevelBase } from '../../../../levels/types';

const base: LevelBase = {
  island: 'hub',
  name: 'hubCombat',
  title: i18n('Combat'),
};

export default base;
