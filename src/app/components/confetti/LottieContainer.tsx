import styled from '@emotion/styled';

const LottieContainer = styled.div`
  position: fixed;
  bottom: 0;
  left: calc(50% - 333px);
  pointer-events: none;
  height: 500px;
  width: 666px;
`;

export default LottieContainer;
